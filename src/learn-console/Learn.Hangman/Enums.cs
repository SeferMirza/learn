namespace Learn.Hangman
{
    public enum GameStatus
    {
        MainMenu,
        Exit,
        Play,
        Over,
        Won
    }
    public enum MenuStatus
    {
        OnOptions,
        OnMenu,
        Done
    }
}
